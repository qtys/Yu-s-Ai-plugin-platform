# 修改记录

本项目遵循语义化版本。日期采用 `YYYY-MM-DD`。

## [0.14.0] - 2026-09-19

- 模型设置新增本地时间上下文开关和位置/地区上下文开关；时间可供模型参考但不强制提及，位置默认关闭且由用户手动填写。
- 主动发言新增“承接最近聊天概率”（默认 15%）；未抽中时不再上传聊天历史，改为按人设随机聊日常、兴趣、轻松话题或可选时事。
- 承接旧话题时仅提供最近两条消息作为轻微线索，并要求模型避免复述、总结或持续追问同一话题。
- 新增对话文档上传，支持 TXT、Markdown、PDF 和 DOCX，单文件最大 20 MB。
- 原文件、提取文本和索引均保存在安装目录数据区；删除文档时同步清理原文件与索引。
- 文档以聊天附件形式集成在输入框内；选择文件后后台自动分批理解，失败可直接在附件卡片重试。
- 聊天时自动组合全文总览与当前问题相关的原文片段，保留 PDF 页码，并限制文档上下文总量。
- 自动提取 DOCX 内嵌图片；PDF 页面完整渲染为整页预览与四象限高清切片，交给兼容 OpenAI 图片输入格式的多模态模型理解。
- 针对 `SCH_`/`SCHEMATIC` 原理图启用硬件分析提示，重点识别元件、引脚、网络、电源域、接口、连接关系和待复核风险。
- 文档图片支持独立视觉模型配置；DeepSeek 官方接口留空时自动使用 `deepseek-flash`，普通聊天模型保持不变。
- 图片分析移除应用侧 16K 输出上限，DeepSeek 视觉请求使用其当前 384K 上限，并继续按页分组，整份文档不设置累计 token 上限。
- 内置 Hugging Face 国内镜像入口，与 Argos 两个官方源自动测速、择优、重试及断点续传。
- 桌宠翻译改为一次下载中译英与英译中两个语言包，并以一个总进度条显示测速、下载和安装进度。
- 普通聊天检测流式响应的 `finish_reason=length`，自动从中断位置续写并合并至同一条回复，最多续写 3 轮，避免用户反复输入“继续”。
- 文档上传新增“快速/深度”分析档位：快速模式仅分析每页整页预览并限制单页输出，深度模式使用整页与四象限高清切片分析原理图细节。
- 文档附件卡新增实时阶段和页码进度，显示文字整理、正在分析第 N/总页、全文汇总及完成状态。
- 快速/深度文档分析模式移入模型设置并持久化到数据库，聊天输入区恢复简洁布局；上传按钮改为别针图标。
- 保持桌宠拖动和主界面视觉效果不变；主窗口不再在每次获得焦点时刷新对话，仅在从桌宠或托盘明确展开时同步，消除首次点击标题栏准备拖动时的数据请求与重绘卡顿。
- Windows WebView2 禁用隐藏窗口的渲染器后台化、定时器节流和遮挡挂起，防止主窗口从桌宠模式恢复后在首次标题栏交互时才唤醒合成线程。
- 修复全局鼠标钩子在桌宠隐藏时仍于每次鼠标移动同步查询窗口状态的问题，避免 Windows 原生标题栏和关闭按钮拖动期间阻塞鼠标消息链。
- 新增 `logs/window-diagnostics.log`，记录主窗口焦点、前端事件循环停顿、WebView 长任务和超过 16 ms 的桌宠鼠标钩子回调，便于复现后精确定位窗口卡顿。
- 托盘退出改为先隐藏窗口、再由后台线程优雅关闭本地服务，并防止退出事件重复等待后端，避免完全退出时界面停顿。
- 文档模型请求失败会把状态码、实际模型和服务端错误详情写入本地日志。
- 文档仅绑定当前对话，不会混入其他角色或对话。

## [0.13.6] - 2026-09-19

- 主动发言继续显示并保存在本地对话中，但增加独立来源标记，不再作为聊天上下文上传给模型。
- 主动互动自身生成新话题时同样排除历史主动发言，避免主动内容反复自我强化。
- 数据库升级自动增加消息来源字段，并重建不含主动发言的对话摘要。

## [0.13.5] - 2026-09-19

- 用户发送手动消息时立即取消正在生成的主动发言，手动对话结束后再重新计算主动发言时间，避免插话和模型请求冲突。
- 随机主动发言支持分别设置最短、最长等待时间，每次触发或重新排程时在区间内重新随机。
- 优化展开界面的消息编辑卡片、输入框、操作按钮和主题色适配，增加字数、快捷键提示及窄窗口布局。
- 主动发言时间配置增加数据库向后兼容迁移与区间合法性校验。

## [0.13.4] - 2026-09-19

- 修复展开界面连续对话后，窗口同步或主动发言插入消息导致流式内容写错位置、回复看似消失的问题。
- 展开界面按固定临时消息标识增量渲染回复，生成期间使用纯文本显示并自动滚动，结束后重新加载持久化消息。
- 流式连接中断时保留已经收到的部分回复，停止或失败后不再遗留空白占位内容。
- 流式解析兼容 `data:` 后有无空格的 SSE，处理末尾缓冲区，并在模型无正文或格式异常时给出明确错误。
- 手动对话和主动发言共用模型生成互斥锁，避免同时请求、消息顺序冲突和上下文污染。
- 用户手动发送消息后，主动发言从当前时刻重新计算空闲间隔，避免紧接着插入主动消息。
- 主动发言启用或修改间隔时从保存时刻重新排程；桌宠按服务端 `next_due` 自适应检查，不再依赖固定一分钟轮询。
- 主动发言请求失败只写日志和设置中的最近错误，不再在桌宠上显示失败气泡。

## [0.13.3] - 2026-09-16

- 后端使用 PyInstaller 目录式打包，运行库随安装包部署到 `backend-runtime`，不再反复向系统 Temp 解压。
- 后端 TEMP/TMP 改到安装目录 `data/temp`，退出先发送正常关闭信号，等待完成后仅在超时时强制结束。
- 父进程退出监控改为正常关闭 Uvicorn，安装升级时也先发送后端关闭信号；用户数据保持保留。
- 增加旧版临时运行库清理脚本，以启动日志、二进制指纹和占用检查核验归属，默认只预览。

## [0.13.2] - 2026-09-16

- 主动发言轮询不再因开关面板、菜单和拖动取消正在生成的气泡；生成结果等待桌宠空闲后显示。
- 模型空正文单独识别，记录 finish_reason 和已报告 token 用量，桌宠及设置显示失败提示。
- 前两次失败约 60 秒后重试，连续失败至少 5 分钟退避，成功后重置失败计数。
- 单次回复额度支持 64–8192 tokens，旧默认 160 自动迁移至 1024。
- 失效的最近对话选择自动回退到新建主动互动对话。

## [0.13.1] - 2026-09-16

- 主动发言间隔支持 1–1440 分钟及常用快捷选项，可选择固定或随机浮动间隔。
- 修改间隔会同步调整剩余冷却，避免旧间隔导致频率设置迟迟不生效。
- 强化角色本人身份、口吻、关系、兴趣和边界，发言类型服从人设，不强行讲笑话或套通用助手台词。
- 启动时先加载主动插件状态，避免模型插件已开启却先弹出通用本地问候。

## [0.13.0] - 2026-09-16

### 新增

- 新增角色主动互动模型插件：结合完整角色卡、本机时间和最近对话生成问题/笑话，并可选择最近 48 小时 RSS 新闻话题。
- 插件默认关闭，可设置调用间隔、输出 token 上限和新闻源；支持冷却、静默时段、usage 统计及对话持久化。
- 空翻拆分为前空翻、后空翻，新增两张独立透明动作素材和透视翻转动画。

## [0.12.0] - 2026-09-12

### 新增

- 离线语言包下载新增流式进度接口，返回已下载字节、总字节、百分比和当前阶段。
- 桌宠翻译面板新增下载进度条，并区分下载中、安装中、完成和失败状态。
- 模型设置新增可选语言包镜像基础地址，支持可信的国内镜像或自建静态文件服务。
- 语言包下载新增官方源/镜像源并行测速、自动择优与故障切换。
- 新增 `.part` 断点续传及单源三次指数退避重试，下载状态会显示测速、重试与续传。

### 体验

- 语言包安装移至后台线程，安装大模型时不会阻塞本地 API 事件循环。
- 未配置镜像时继续使用 Argos 官方下载地址，也可通过 `YUS_AI_TRANSLATION_MIRROR` 环境变量覆盖。

### 修复

- 桌宠收起及功能圈状态下，透明空白区域不再阻挡后方软件的鼠标操作。
- 下载进度与来源提示限制在翻译面板内，过长来源名称以省略号显示。
- 已打开的桌宠对话、翻译或设置面板改为单击桌宠即可关闭。
- 新增闭眼、眨眼、惊喜三套透明桌宠动作帧，并加入随机眨眼、点击表情反馈与待机空翻动作。

## [0.11.0] - 2026-09-12

### 新增

- 桌宠读取本机时间并区分早晨、白天、傍晚和深夜，自动调整视觉状态与待机动作倾向。
- 主动台词结合当前角色的开场白、性格、说话方式和用户关系称呼，本地生成且不会额外调用模型。
- 桌宠设置新增时间感知和角色台词独立开关；深夜时间段默认不主动打扰。
- 对话框支持横向与纵向拖拽，也可在设置中精确调整并保存宽度和高度。

### 修复

- 缩小桌宠透明点击判定框，同时保持视觉尺寸不变，减少对桌面点击的误拦截。
- 对话框宽高变化时同步调整原生透明窗口，并保持桌宠屏幕锚点不变。

## [0.10.0] - 2026-09-12

### 新增

- 桌宠会根据鼠标位置轻微偏转，点击时产生弹性挤压和水波反馈。
- 拖动桌宠时身体会拉伸，松手后回弹；靠近显示器边缘时自动吸附。
- 闲置桌宠会随机播放挤压、摇晃和犯困动作。
- 模型或翻译成功完成时播放开心动作，发生错误时播放疑惑动作。
- 新增主动关怀冒泡，定时显示喝水、久坐休息等提醒，并可在桌宠设置中关闭。

### 体验

- 主动冒泡不会展开大型透明窗口，并可单独关闭，不影响桌面其余区域点击。
- 桌宠靠屏幕边缘吸附后，功能圈继续朝屏幕内侧展开。

## [0.9.0] - 2026-09-12

### 新增

- 展开界面的模型设置新增“消息显示插件”，支持 Markdown 渲染、Markdown 过滤和原始文本三种互斥模式。
- Markdown 渲染支持 GFM 标题、列表、任务列表、表格、引用、链接和代码块。
- Markdown 过滤模式移除常见格式标记，仅保留便于阅读的纯文本内容。
- 显示模式保存到 SQLite，并由主界面和桌宠共同使用。
- 桌宠功能圈新增独立“设置”入口，将桌宠大小、透明度和对话文字大小从聊天气泡中集中移入设置面板。
- 五个功能圈改为从正下方开始、按 45° 间隔均分 180° 的半圆时钟布局；靠近屏幕边缘时向内侧镜像。
- 双击尚未展开功能圈的桌宠，可直接恢复上一次关闭的对话、翻译或设置功能，并跨重启记忆选择。

### 修复

- 面板切换时先平滑隐藏界面，在原生窗口尺寸、位置和 React 状态同步后再显示，修复桌宠与气泡先后闪烁的问题。
- 区分桌宠单击与双击事件，双击直接打开功能时不再短暂弹出功能圈。

### 安全

- Markdown 使用安全 React 节点渲染，不启用原始 HTML，也不执行模型回复中的脚本。

## [0.8.1] - 2026-09-12

### 修复

- 增加进程级单实例保护，连续点击快捷方式或开机启动重复触发时不会再创建第二个程序、后端或托盘图标。
- 再次启动时唤醒现有实例：主界面可见时将其置前，否则显示并聚焦桌宠。

## [0.8.0] - 2026-09-12

### 新增

- 模型设置新增开机自启开关，系统托盘菜单也可随时开启或关闭，并保持状态同步。
- 开机自启后默认进入桌宠模式，并恢复上次保存的位置。

### 安装与升级

- 使用 Tauri 官方 autostart 插件维护当前用户的 Windows 启动项，无需管理员权限。
- 覆盖升级时保留用户选择；如果安装路径发生变化，会更新已有启动项指向新路径。
- 卸载时删除 `Run` 与 `StartupApproved` 中的 `Yus AI` 项，避免残留失效注册表记录。

## [0.7.1] - 2026-09-12

### 修复

- 连续翻译忽略桌宠翻译框自身的文本选择，复制译文时不再重复触发翻译。
- 首次下载语言包成功后立即更新当前语言对的安装状态，并通过无缓存查询二次校准。

## [0.7.0] - 2026-09-12

### 新增

- 桌宠离线翻译面板新增连续翻译模式；开启后，在其他 Windows 应用中用鼠标选中文本即可自动翻译。
- 连续翻译自动识别中英文方向，并将原文和译文实时更新到桌宠翻译框。

### 安全与体验

- 连续模式仅在用户主动开启时监听鼠标选择，关闭翻译框、隐藏桌宠或返回主界面时自动停止。
- 全局鼠标钩子仅投递选择事件，复制与翻译在后台线程执行，避免阻塞正常鼠标操作。
- 通过模拟标准 `Ctrl+C` 获取选区；原剪贴板为文本时会在读取后自动恢复。

## [0.6.1] - 2026-09-12

### 修复

- 桌宠启动时先等待本地服务并恢复数据库坐标，再显示透明窗口，避免短暂或持续出现在默认位置。
- 在位置恢复完成前忽略窗口移动事件，防止启动阶段把默认坐标覆盖到数据库。

## [0.6.0] - 2026-09-11

### 新增

- 用户消息和 AI 回复支持原地编辑，并将修改后的内容持久化到 SQLite。
- 保存消息修改后，后续模型请求会使用修改后的历史内容作为上下文。

### 变更

- 编辑历史消息时清除旧的对话摘要，避免后续上下文继续引用修改前的内容。
- 编辑用户消息时同步重建由该消息产生的长期记忆。
- 流式回复结束后重新同步消息记录，使新回复无需刷新即可立即编辑。

## [0.5.2] - 2026-09-11

### 修复

- 统一托盘、主窗口按钮和窗口关闭时的桌宠切换逻辑，避免主界面与桌宠同时显示。
- 隐藏或重新显示桌宠时同步重置对话、翻译和功能圈状态，修复展开窗口按收起尺寸显示而导致气泡被裁切的问题。
- 确保每次数据库操作后关闭 SQLite 连接，避免 Windows 文件锁残留。

## [0.5.1] - 2026-09-11

### 修复

- 修复重复点击当前角色时最近对话被清空且不会重新加载的问题；重复点击现在会主动刷新该角色的对话列表。

## [0.5.0] - 2026-09-11

### 新增

- 模型设置支持手动调整最近上下文消息数（2–200）和相关长期记忆条数（0–50）。
- 长对话自动保留较早内容摘要，并仅发送配置数量的最近消息。
- 角色长期记忆按角色隔离，从用户明确要求记住的姓名、偏好和个人信息中自动提取，并按当前话题相关度注入。
- 最近对话支持手动重命名。

## [0.4.1] - 2026-09-11

### 修复

- 桌面端角色导出改为原生文件写入，修复 WebView 模拟下载无响应的问题。
- 导出文件保存到安装目录 `data/character-exports`，使用安全文件名和时间戳避免覆盖。

## [0.4.0] - 2026-09-11

### 新增

- 角色卡支持头像、开场白、身份背景、性格、说话方式、用户关系、行为边界和示例对话。
- 结构化字段会自动编译为系统提示词，同时兼容原有补充提示词。
- 支持编辑、删除、JSON 导入和导出角色；删除角色会级联删除所属对话。
- 新对话自动写入角色开场白，角色头像用于侧栏展示。

### 兼容

- 已有数据库启动时自动添加角色卡字段，原有角色和对话无需迁移操作。


## [0.3.1] - 2026-09-11

### 新增

- 对话与翻译面板新增独立文字大小调节，并在本机保存选择。
- 系统托盘菜单新增桌宠放大、缩小、“更清晰”和“更透明”操作。

### 修复

- 桌宠启动时自动重试连接本地服务，连接成功后立即刷新等待提示，无需先发送消息。

## [0.3.0] - 2026-09-11

### 新增

- 桌宠功能圈新增“翻译”入口，以及可独立关闭的离线翻译面板。
- 集成 Argos Translate 本地翻译引擎，首期支持中文与英文双向翻译。
- 中英、英中语言包按需分别下载，并显示预计下载体积和安装状态。
- 翻译模型保存在安装目录的 `data/translation-models`，升级时保留，安装完成后无需联网。

## [0.2.3] - 2026-09-11

### 新增

- 桌宠位置写入安装目录中的 SQLite 数据库，重新启动后恢复上次位置。
- 桌宠功能圈新增“关闭”入口；系统托盘新增独立的“显示/隐藏桌宠”操作。

### 修复

- 对话气泡增加明确的关闭按钮，并恢复双击桌宠关闭气泡的操作。
- 主窗口关闭后自动切换到桌宠模式，只有托盘“退出”会结束整个程序。
- 缩短气泡与桌宠之间的距离，同时保持二者不重叠。
- 修复点击桌宠边缘时因焦点刷新和轻微鼠标抖动导致桌宠跳回旧位置的问题。
- 改为监听原生窗口移动结束事件，并以桌宠自身坐标写入数据库，确保重启后准确恢复位置。

### 变更

- 应用启动时默认显示桌宠，不再默认打开完整主窗口。
- 移除桌宠下方的操作提示标签。

## [0.2.2] - 2026-09-11

### 变更

- 移除固定拖动按钮和长按等待，按住史莱姆移动鼠标即可立即拖动窗口。
- 桌宠交互改为双击展开气泡、再次双击收起，避免单击误触。
- 调整桌宠大小时同步缩放气泡、文字、输入控件、精灵和透明窗口，保持相对位置一致。
- 桌宠气泡展开、缩放和拖动结束时会根据当前显示器边界自动校正位置，避免超出屏幕。
- 气泡会根据桌宠所在位置在左上、右上、左下、右下四种布局间切换，切换时保持桌宠的屏幕位置，并与桌宠留出间距。
- 单击桌宠会在下方展开圆形功能入口；“对话”入口用于打开气泡，并预留插件入口供后续功能扩展。
- 桌面版数据库改为保存在安装目录的 `data` 文件夹，覆盖升级时持续保留。
- 首次运行新版时自动从旧版 AppData 位置复制数据库，并保留旧文件作为安全备份。

## [0.2.1] - 2026-09-11

### 新增

- 桌宠气泡新增大小和透明度滑块，选择会保存在本机。
- 桌宠新增明确的拖动手柄，可在屏幕上自由移动。

### 修复

- 收起气泡时同步缩小透明窗口，避免不可见区域继续拦截桌面点击。
- 返回主窗口时重新载入桌宠使用的角色、会话和消息，桌宠对话现在可在完整界面查看。
- 桌宠窗口展开和收起时保持右下锚点，避免精灵在屏幕上突然跳位。

### 变更

- 蓝色雨滴精灵更新为更圆润的史莱姆造型，移除独立手脚。

## [0.2.0] - 2026-09-11

### 新增

- 新增独立的透明桌面宠物窗口，默认使用原创蓝色雨滴精灵形象。
- 点击精灵展开快捷输入，AI 流式回复显示在漫画式对话气泡中。
- 桌宠提供待机漂浮和思考动画，支持拖动、始终置顶及跳过任务栏。
- 双击精灵或点击气泡“展开”可返回完整聊天主窗口。
- 主界面会把当前角色和会话同步给桌宠，继续使用同一份本地聊天数据。

### 变更

- 原“迷你”入口升级为“桌宠”入口；进入后隐藏主窗口并显示桌宠。

## [0.1.4] - 2026-09-10

### 新增

- 最近对话条目新增删除按钮，删除前会二次确认，并同步删除该会话的全部消息。

### 修复

- 启动时在恢复角色后立即加载其最近对话，无需先发送消息触发刷新。
- 修复模型设置内容超出桌面窗口后无法滚动的问题。
- 删除当前对话后会自动切换至下一条对话；没有剩余对话时回到空白聊天页。

## [0.1.3] - 2026-09-10

### 修复

- 主题现在同步应用于品牌图标、角色与消息头像、正文、标签、状态灯和输入区域。
- 修复模型设置页面向下滚动时露出黑色根背景的问题。
- 修复新会话发送第一句话时，空历史请求覆盖用户消息的问题。
- 恢复首轮用户消息与 AI 回复之间正确的消息顺序和间距。

## [0.1.2] - 2026-09-10

### 修复

- 托盘“退出”现在会在关闭主程序前主动终止本地后端。
- 后端新增父进程监视；主程序崩溃或被强制结束后，后端也会自动退出。
- Windows 安装和卸载前自动清理旧版主程序与后端进程，避免升级时无法覆盖可执行文件。

## [0.1.1] - 2026-09-10

### 新增

- 新增暮紫、午夜蓝、暖砂和纸墨四套界面主题，并在本机保存用户选择。
- 聊天输入框会随内容自动增高，达到最大高度后再显示滚动条。

### 修复

- 修复展开窗口后置顶按钮上半部分越出标题栏的问题。
- 统一输入框滚动条视觉，隐藏与界面风格不一致的原生上下箭头。
- 改进窄窗口标题和顶部操作按钮的收缩行为。

## [0.1.0] - 2026-08-28

### 新增

- React + TypeScript 响应式聊天界面。
- FastAPI + SQLite 本地后端。
- OpenAI Chat Completions 兼容模型接口与流式回复。
- 角色卡、系统提示词、多会话和消息历史持久化。
- 模型地址、API Key、模型名称、温度和最大输出配置。
- Tauri 2 Windows 桌面外壳。
- 窗口始终置顶、迷你聊天窗口和系统托盘。
- PyInstaller 后端 sidecar，桌面应用自动启动和关闭本地服务。
- Windows NSIS 安装包构建流程。
- 轮转文件日志、sidecar 启动日志和安全诊断 API。
- 数据库检查、WAL checkpoint、模型连接测试及日志标记指令。
- 后端健康检查等待，避免桌面界面早于本地服务启动。

### 修复

- 修复 PyInstaller 未包含 `app` 包导致安装版后端无法启动的问题。
- 修复旧 sidecar 占用 8000 端口时缺乏可诊断信息的问题。
- 修复 HTTPX 继承异常代理环境导致 DeepSeek TLS `ConnectError` 的问题。
- 修复安装目录日志缺失；现在优先写入安装位置的 `logs` 目录。
- 修复角色创建时后端尚未就绪导致的 `Failed to fetch`。

### 安全

- 日志不记录 API Key、Authorization Header、聊天正文和系统提示词。
- 诊断 API 仅支持白名单操作，不提供任意 Shell 或 PowerShell 执行。
- 数据库、日志、构建产物和本机配置均已加入 Git 忽略规则。
