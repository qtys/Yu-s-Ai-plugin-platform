# Yu's AI Plugin Platform

Yu's AI 是一款本地优先的 Windows 私人 AI 聊天工具。它将角色扮演、多模型 API、长期记忆、离线翻译和桌面宠物整合在同一个应用中，并为后续插件能力预留扩展空间。

当前版本：**0.9.0**

## 0.9.0 版本亮点

- 新增消息显示插件，可选择渲染 Markdown、过滤 Markdown 或查看原始文本
- 支持 GFM 列表、任务列表、表格、引用、链接及代码块
- 插件选择保存在本地数据库，主界面与桌宠使用相同显示模式
- Markdown 回复以安全 React 节点渲染，不执行原始 HTML 或脚本
- 桌宠新增独立“设置”功能圈，集中调整桌宠大小、透明度和对话文字大小
- 五个功能圈按时钟刻度均分 180° 环绕桌宠，并在屏幕边缘自动朝内侧展开
- 优化桌宠面板切换动画，双击桌宠可直接恢复上一次使用的功能

## 主要功能

### 私人 AI 对话

- 支持 OpenAI Chat Completions 兼容接口，可连接 DeepSeek 等兼容服务
- 支持流式回复、停止生成、多会话和本地历史记录
- 可配置 API 地址、API Key、模型名称、温度和最大输出长度
- 最近对话支持重命名和删除
- 用户消息与 AI 回复均可修改，后续对话会使用修改后的内容作为上下文
- 模型设置提供 Markdown 渲染、Markdown 过滤和原始文本三种消息显示插件
- 提供暮紫、午夜蓝、暖砂和纸墨四套界面主题

### 角色卡与记忆

- 角色卡支持头像、开场白、身份背景、性格、说话方式、用户关系、行为边界和示例对话
- 结构化角色信息会自动生成系统提示词，也可继续填写自定义提示词
- 支持创建、编辑、删除，以及 JSON 格式的角色导入和导出
- 可手动设置最近上下文消息数（2–200）和相关长期记忆条数（0–50）
- 长对话会保留较早内容摘要，并按角色隔离长期记忆
- 可从用户明确要求记住的信息中提取姓名、偏好和个人资料，并按当前话题选择相关记忆

### 蓝色雨滴桌宠

- 应用启动后默认显示透明、无边框且始终置顶的蓝色史莱姆桌宠
- 按住桌宠并移动鼠标即可拖动，位置会在下次启动时恢复
- 单击桌宠展开功能圈；五个功能圈从正下方开始沿 180° 半圆等距环绕桌宠
- 双击未展开功能圈的桌宠，可直接打开上一次使用的对话、翻译或设置面板
- 对话、翻译等面板可以独立关闭，隐藏区域不会继续拦截桌面点击
- “设置”功能圈集中提供桌宠大小、透明度和对话文字大小调节，选择会自动保存
- 面板打开和关闭时会同步切换原生窗口与界面布局，避免桌宠和气泡跳动闪烁
- 气泡会根据屏幕边界调整布局，同时保持靠近桌宠且不与桌宠重叠
- 桌宠与主窗口共用角色、会话和消息记录
- 可从模型设置或系统托盘开启、关闭 Windows 开机自启
- 程序采用单实例运行，重复启动只会唤醒已有窗口，不会重复启动本地服务

### 离线翻译

- 基于 Argos Translate 提供中文与英文双向翻译
- 中英、英中语言包可按需单独下载
- 语言包安装完成后无需联网，模型保存在安装目录中
- Windows 桌面版支持连续翻译：开启后，在其他应用中用鼠标选中文本即可自动识别方向并显示译文

#### 使用连续翻译

1. 单击桌宠，打开功能圈中的“翻译”。
2. 如果尚未安装当前方向的语言包，先点击下载；下载成功后状态会立即刷新。
3. 点击翻译框顶部的“○ 连续”，显示为“● 连续”即表示已经开启。
4. 切换到浏览器、记事本或其他应用，用鼠标选中一段中文或英文。
5. 桌宠会自动识别中英文方向，并在翻译框中更新原文和译文。

选择桌宠翻译框自身的文字不会触发新翻译。关闭翻译框、隐藏桌宠或返回主界面时，连续翻译会自动停止。

## 安装与使用

项目当前提供 Windows NSIS 安装包。自行构建后，安装包位于：

```text
frontend\src-tauri\target\release\bundle\nsis\Yus AI_0.9.0_x64-setup.exe
```

安装后无需打开终端，直接启动 **Yus AI** 即可：

1. 从系统托盘打开主界面。
2. 创建或导入一个角色。
3. 在“模型设置”中填写兼容 API 地址、API Key 和模型名称。
4. 返回聊天页面创建对话并发送消息。

### 消息显示插件

在“模型设置 → 消息显示插件”中选择一种模式，然后点击“保存设置”：

- **Markdown 渲染**：默认模式，将模型回复中的格式转换为标题、列表、表格、引用和代码块
- **Markdown 过滤**：移除常见 Markdown 标记，只显示纯文本
- **原始文本**：不做处理，完整显示模型返回的标记，适合调试提示词和输出

三种模式互斥，不会发生先渲染再过滤的冲突。当前选择保存在 SQLite，桌宠回复也会同步使用。

关闭主窗口不会退出程序，而是切换回桌宠模式。需要彻底结束程序时，请在系统托盘右键菜单中选择“退出”。托盘菜单也可以显示或隐藏桌宠，并调整桌宠大小和透明度。

### 开机自启与单实例

- 在“模型设置 → 系统启动”或系统托盘菜单中开启、关闭开机自启
- 开机启动后直接进入桌宠模式，并恢复上次保存的位置
- 连续点击快捷方式不会启动第二个程序、第二个后端或第二个托盘图标
- 再次启动时，如果主界面已经显示则将它置前，否则显示现有桌宠
- 启动项写入当前用户的 `HKEY_CURRENT_USER\Software\Microsoft\Windows\CurrentVersion\Run`，不需要管理员权限
- 覆盖升级会保留自启选择，并在安装路径变化时修正启动路径；卸载会清除相关注册表项

## 版本历史

以下为每个已发布版本的主要更新；更完整的新增、修复和安全说明见 [CHANGELOG.md](CHANGELOG.md)。

### 0.9.0（2026-09-12）

- 新增 Markdown 渲染、Markdown 过滤和原始文本三种消息显示模式，主界面与桌宠共享设置。
- 新增独立桌宠设置圈；五个功能圈按 180° 半圆时钟刻度排列。
- 修复面板切换闪烁，支持双击直接恢复上一次使用的桌宠功能。

### 0.8.1（2026-09-12）

- 增加进程级单实例保护，重复启动只会唤醒现有主窗口或桌宠。

### 0.8.0（2026-09-12）

- 新增模型设置与系统托盘开机自启开关。
- 覆盖升级时保留自启选择并修正安装路径，卸载时清理启动项。

### 0.7.1（2026-09-12）

- 连续翻译忽略桌宠翻译框自身的文本选择。
- 修复首次下载语言包后安装状态不能立即刷新的问题。

### 0.7.0（2026-09-12）

- 新增连续翻译模式，在其他 Windows 应用中选中文本即可自动翻译。
- 自动识别中英文方向，并安全管理鼠标监听与剪贴板恢复。

### 0.6.1（2026-09-12）

- 修复桌宠启动时先出现在默认位置、稍后才恢复保存位置的问题。

### 0.6.0（2026-09-11）

- 支持修改用户消息与 AI 回复，后续对话使用修改后的历史作为上下文。
- 修改消息后自动清理旧摘要，并同步重建相关长期记忆。

### 0.5.2（2026-09-11）

- 修复主窗口与桌宠同时显示、气泡被裁切以及 SQLite 连接残留问题。

### 0.5.1（2026-09-11）

- 修复重复点击当前角色后最近对话不再显示的问题。

### 0.5.0（2026-09-11）

- 新增可调上下文长度、相关记忆条数、长对话摘要和角色长期记忆。
- 最近对话支持手动重命名。

### 0.4.1（2026-09-11）

- 角色导出改用桌面原生文件写入，并保存到安装目录的角色导出文件夹。

### 0.4.0（2026-09-11）

- 完善结构化角色卡，支持头像、开场白、性格、关系、边界和示例对话。
- 支持角色创建、编辑、删除以及 JSON 导入导出，并兼容已有数据库。

### 0.3.1（2026-09-11）

- 新增对话与翻译文字大小调节，以及系统托盘桌宠大小和透明度操作。
- 本地服务准备完成后自动刷新桌宠等待提示。

### 0.3.0（2026-09-11）

- 新增基于 Argos Translate 的桌宠离线翻译，中英语言包可按需下载并持久保存。

### 0.2.3（2026-09-11）

- 桌宠位置保存到 SQLite，新增桌宠与托盘关闭/显示入口。
- 修复气泡关闭、桌宠拖动跳位和退出逻辑，并默认以桌宠模式启动。

### 0.2.2（2026-09-11）

- 桌宠支持直接按住拖动、双击开关气泡、边界布局校正和功能圈入口。
- 数据库迁移到安装目录并在覆盖升级时保留，同时兼容旧 AppData 数据。

### 0.2.1（2026-09-11）

- 新增桌宠大小、透明度和拖动调节，史莱姆造型更加圆润。
- 修复透明窗口残留点击区域、聊天记录不同步和缩放跳位问题。

### 0.2.0（2026-09-11）

- 首次加入透明置顶桌宠窗口、漫画气泡、流式回复、动画和主界面会话同步。

### 0.1.4（2026-09-10）

- 最近对话支持手动删除。
- 修复首次加载最近对话、模型设置滚动以及删除后的会话切换。

### 0.1.3（2026-09-10）

- 完善主题颜色覆盖，修复模型设置黑块和新对话首轮消息显示顺序。

### 0.1.2（2026-09-10）

- 完善主程序、后端和安装器的进程清理，永久解决升级时可执行文件被占用的问题。

### 0.1.1（2026-09-10）

- 新增四套界面主题和自动增高输入框。
- 修复置顶按钮越界及输入框原生滚动按钮样式问题。

### 0.1.0（2026-08-28）

- 建立 React、FastAPI、SQLite、Tauri 和 PyInstaller 桌面应用基础架构。
- 实现角色、多会话、流式模型回复、设置、日志、诊断接口与 NSIS 安装流程。
- 修复安装版后端加载、端口占用、代理连接、安装目录日志和启动时序问题。

## 本地数据与隐私

桌面版的运行数据保存在软件安装目录中，覆盖安装或升级时不会删除：

```text
<安装目录>\data\yus_ai.db
<安装目录>\data\translation-models\
<安装目录>\data\character-exports\
<安装目录>\logs\
```

- `yus_ai.db`：角色、会话、消息、设置、桌宠位置和记忆
- `translation-models`：离线翻译语言包
- `character-exports`：导出的角色卡
- `logs`：后端运行日志和诊断信息

首次运行新版时，如果安装目录内还没有数据库，程序会尝试从旧版 Windows 用户应用数据目录复制已有数据，并保留旧文件作为备份。

日志不会记录 API Key、Authorization Header、聊天正文或系统提示词。需要排查问题时，可参考 [调试接口](docs/DEBUG_API.md)。

> 注意：API Key 当前保存在本地 SQLite 数据库中，尚未接入 Windows Credential Manager。请勿将 `data`、`logs` 或本机配置文件上传到公开仓库。

## 本地开发

### 环境要求

- Windows 10/11
- Node.js 20+
- Python 3.10+
- Rust 与 Tauri 2 所需的 Windows 开发环境

### 启动后端

在项目根目录运行：

```powershell
python -m venv .venv
.\.venv\Scripts\python -m pip install -r backend\requirements.txt
cd backend
..\.venv\Scripts\python -m uvicorn app.main:app --reload
```

后端地址为 `http://localhost:8000`，交互式接口文档位于 `http://localhost:8000/docs`。

### 启动网页开发界面

另开一个终端：

```powershell
cd frontend
npm install
npm run dev
```

浏览器打开 `http://localhost:5173`。网页开发模式主要用于界面调试，不包含完整的桌宠、系统托盘和原生文件能力。

### 启动桌面开发模式

先构建本地后端 sidecar：

```powershell
.\.venv\Scripts\python -m pip install -r backend\requirements-build.txt
.\scripts\build-backend.ps1
cd frontend
npm run desktop:dev
```

### 生成 Windows 安装包

```powershell
cd frontend
npm run desktop:build
```

## 测试

前端构建：

```powershell
cd frontend
npm run build
```

后端测试：

```powershell
cd backend
..\.venv\Scripts\python -m pytest -q
```

网页开发模式的数据写入 `backend/data/yus_ai.db`；桌面版数据写入软件安装目录，两者均已加入 Git 忽略规则。

## 当前限制

- 当前只提供 Windows 桌面安装包
- 本地后端固定监听 `127.0.0.1:8000`
- API Key 暂时以本地数据库形式保存
- 目前内置插件能力以离线翻译为主，通用插件系统仍在逐步建设
- 连续翻译依赖目标应用支持标准 `Ctrl+C` 复制；管理员窗口、受保护页面或特殊绘制界面可能无法读取选区
- 连续翻译可恢复原来的文本剪贴板；如果原剪贴板只有图片或文件，目标应用执行复制后可能改变剪贴板内容

## 项目文档

- [MVP 规格](docs/MVP.md)
- [项目进度](docs/PROGRESS.md)
- [修改记录](CHANGELOG.md)
- [架构说明](docs/ARCHITECTURE.md)
- [调试接口](docs/DEBUG_API.md)
